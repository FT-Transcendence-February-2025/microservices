export const playersController = {
    
    async initPlayersTable (request, reply) {
        console.log(`PLayers table init...`);
        // for each tournament
        // first added players is host
        // in request tournament id and user id
      }
}